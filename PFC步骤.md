# PFC 步骤

使用硬件平台：单相电压型 PWM 整流器

1. 现在，需要采集 AC 输入端的输入电压和电流，还有负载上的电压，它们可以采用使用互感器的交流采样模块和分压后接一个运放电压跟随来搞定，这意味着要 3 个单片机的 ADC 来处理数据，现在配置有 4 个 ADC 注入组通道，第 0 个是逆变器用的，剩下三个可以用来采集这三个信号。采集完之后，有三个重要目标。

2. 目标 1 是锁相，通过读取得到的输入电压推算出电压的相位角 theta。采用 SOGI-PLL。

3. 目标 2 是电压电流双闭环，外环是 PID(目标电压 - 负载电压)，内环是 PID(外环结果 * cos theta - 输入电流)。最终计算 (输入电压 - 内环结果) / 目标电压，可以得到处于 [-1, 1] 的结果量。

4. 目标 3 以 (双闭环的结果 + 1) / 2 作为占空比，生成 PWM 调制波形，控制全桥 MOSFET 导通。
